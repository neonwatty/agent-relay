# Agent Relay Codex Plugin

This plugin teaches Codex agents how to use the `agent-relay` CLI as a local coordination layer.

It does not record transcripts, scan files, or publish automatically. Agents only write explicit events, claims, notifications, and handoffs.
